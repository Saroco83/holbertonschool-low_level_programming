#include <stdio.h>

/**
 * main - function ppal
 * @argc: counter
 * @argv: values, strings
 * Return: 0 integer value
 */
int main(int argc, char *argv[])
{
	if (argc >= 0)
		printf("%s\n", argv[0]);

	return (0);

}
