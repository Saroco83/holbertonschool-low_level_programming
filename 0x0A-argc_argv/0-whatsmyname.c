#include <stdio.h>

/**
 * main - Entry point
 * @argc: int variable
 * @argv: char* variable
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
