#include <stdio.h>

/**
 * main - function ppal.
 * @argc: counter.
 * @argv: values, strings.
 * Return: integer value.
 */
int main(int argc, char *argv[])
{
	if (argc >= 0)
	printf("%s\n", argv[argc - 1]);
	return (0);

}
