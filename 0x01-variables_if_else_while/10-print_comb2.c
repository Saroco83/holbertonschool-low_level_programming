#include <stdio.h>
/**
 * main - function ppal.
 * Return: 0 on succes.
 */
int main(void)
{
	int i, j;

	for (i = '0'; i <= '9'; i++)
	{	putchar(i);
	putchar(j);
		for (j = '0'; j <= '9'; i++)
*		{
*		*		/**	if 
*		*	(i != '9' && j != '9')
*		*	{
*		*		putchar(44);
*		*		putchar(32);
*		*	
*		}
*/	}
	putchar('\n');
	return (0);
}
